import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'code.isand.git',
  appName: 'git_Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
